import {JubDID, KeyPair, createHDKeyPair,generateRandomMnemonic} from "./JubDID";
import {JubRegistry} from "./registry";
import {JubResolver} from "./resolver";
import {verifyCredential} from "did-jwt-vc";


export {JubDID,KeyPair,JubRegistry,JubResolver,createHDKeyPair,generateRandomMnemonic, verifyCredential};
